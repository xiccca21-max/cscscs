import type { ReactNode } from "react";

export const metadata = { title: "Referral Dashboard - SKINSELL" };

export default function ReferralLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
